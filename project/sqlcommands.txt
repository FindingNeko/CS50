CREATE TABLE prints (
    id INTEGER NOT NULL PRIMARY KEY,
    cb0 BINARY,
    cb1 BINARY,
    cb2 BINARY,
    cb3 BINARY,
    cb4 BINARY,
    cb5 BINARY,
    cb6 BINARY,
    cb7 BINARY,
    cb8 BINARY,
    cb9 BINARY,
    cb10 BINARY,
    cb11 BINARY,
    cb12 BINARY,
    cb13 BINARY,
    cb14 BINARY,
    cb15 BINARY,
    cb16 BINARY,
    cb17 BINARY,
    cb18 BINARY,
    cb19 BINARY,
    cb20 BINARY,
    cb21 BINARY,
    cb22 BINARY,
    cb23 BINARY,
    cb24 BINARY,
    cb25 BINARY,
    cb26 BINARY,
    cb27 BINARY,
    cb28 BINARY,
    cb29 BINARY,
    cb30 BINARY,
    cb31 BINARY,
    cb32 BINARY,
    cb33 BINARY,
    cb34 BINARY,
    cb35 BINARY,
    cb36 BINARY,
    cb37 BINARY,
    cb38 BINARY,
    cb39 BINARY,
    cb40 BINARY,
    cb41 BINARY,
    cb42 BINARY,
    cb43 BINARY,
    cb44 BINARY,
    cb45 BINARY,
    cb46 BINARY,
    cb47 BINARY,
    cb48 BINARY,
    cb49 BINARY,
    cb50 BINARY,
    cb51 BINARY,
    cb52 BINARY,
    cb53 BINARY,
    cb54 BINARY,
    cb55 BINARY,
    cb56 BINARY,
    cb57 BINARY,
    cb58 BINARY,
    cb59 BINARY,
    cb60 BINARY,
    cb61 BINARY,
    cb62 BINARY,
    cb63 BINARY,
    cb64 BINARY,
    cb65 BINARY,
    cb66 BINARY,
    cb67 BINARY,
    cb68 BINARY,
    cb69 BINARY,
    cb70 BINARY,
    cb71 BINARY,
    cb72 BINARY,
    cb73 BINARY,
    cb74 BINARY,
    cb75 BINARY,
    cb76 BINARY,
    cb77 BINARY,
    cb78 BINARY,
    cb79 BINARY,
    cb80 BINARY,
    cb81 BINARY,
    cb82 BINARY,
    cb83 BINARY,
    cb84 BINARY,
    cb85 BINARY,
    cb86 BINARY,
    cb87 BINARY,
    cb88 BINARY,
    cb89 BINARY,
    cb90 BINARY,
    cb91 BINARY,
    cb92 BINARY,
    cb93 BINARY,
    cb94 BINARY,
    cb95 BINARY,
    cb96 BINARY,
    cb97 BINARY,
    cb98 BINARY,
    cb99 BINARY
);